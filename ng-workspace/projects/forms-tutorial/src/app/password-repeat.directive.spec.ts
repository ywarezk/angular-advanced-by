import { PasswordRepeatDirective } from './password-repeat.directive';

describe('PasswordRepeatDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordRepeatDirective();
    expect(directive).toBeTruthy();
  });
});
